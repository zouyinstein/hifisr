"""Read recruitment, binning, and statistics helpers.

The package name is `read_ops` instead of `reads` so the legacy
`hifisr_functions.reads` module keeps its existing import behavior.
"""

__all__ = ["binning", "recruit", "stats"]
